name: CI - FluidAttacks (SAST + SCA) - BREAKING
on:
  workflow_dispatch: {}
  pull_request: {}
  push:
    branches:
      - main
      - master
  schedule:
    - cron: "12 15 * * *"

jobs:
  fluidattacks_scans:
    name: FluidAttacks scans (SAST + SCA) - STRICT
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: FluidAttacks SAST (Docker image) - Fail on Vulnerabilities
        id: sast
        uses: docker://docker.io/fluidattacks/sast:latest
        with:
          args: sast --strict scan .

      - name: FluidAttacks SCA (Docker image) - Fail on Vulnerabilities
        id: sca
        uses: docker://docker.io/fluidattacks/sca:latest
        with:
          args: sca --strict scan .

      - name: FluidAttacks summary
        run: |
          echo "✅ FluidAttacks scans finished (SAST + SCA) - NO VULNERABILITIES FOUND"
          echo "All scans passed successfully!"

  semgrep:
    name: semgrep/ci - STRICT
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
    container:
      image: semgrep/semgrep
    if: (github.actor != 'dependabot[bot]')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run semgrep CI
        run: |
          semgrep ci
          exit_code=$?
          if [ $exit_code -ne 0 ]; then
            echo "::error::Semgrep found security issues!"
            exit 1
          fi
